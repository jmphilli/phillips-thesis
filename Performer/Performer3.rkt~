#lang FrTime
(require "./Performer-Interface.rkt")

#|
(define (sum . xs)
    (if (null? xs)
        0
        (+ (car xs) (apply sum (cdr xs)))))
|#

(define (new-func . tl) (car tl))

(set! music-function new-func)

(provide perform
         update-music-signal
         init-performer)